// Placeholder crate root. quilt-lsp overlays the projection of `main.rs.quilt`
// on top of this file in-memory, so rust-analyzer analyzes it inside this crate.
// (Run `quilt expand src/main.rs.quilt` to generate the real contents here.)
fn main() {}
