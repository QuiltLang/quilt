#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT/quilt"
exec cargo run --manifest-path "$ROOT/Cargo.toml" -p quiltlang --no-default-features -F rust,parse -- run -m omni src/langs/bootstrap/mk_meta.rs.quilt "$@"
